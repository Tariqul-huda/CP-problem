#include "bits/stdc++.h"
using namespace std;
#define int long long
#define endl '\n'
signed main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int T;
    cin >> T;
    while (T--) {
      int sum  = 0;
      int n;
      cin >> n;
      int a[n];
      for(int i = 0;i<n;i++)
      {
        cin >> a[i];
      }
      
    }
    return 0;
}